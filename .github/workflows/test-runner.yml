name: Test Self Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Display Files
        shell: powershell
        run: |
          Write-Host "Runner is working!"
          Get-ChildItem